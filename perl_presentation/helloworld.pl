#!/usr/bin/perl 
  
#FLAGS
#requires all variables to be initialized
use strict; 
#sends warnings when the compiler can predict potential bugs (such as when variables aren't initialized)
use warnings; 
  
#print is built in
print("Hello World\n");
